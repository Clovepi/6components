import React, { useState } from "react";

export default function counter() {
    const [ count, Zero ] = useState(0);

    return (
        <div className="motivation">
            <h1>it's okay cass you can do it</h1>
        </div>
    )
}